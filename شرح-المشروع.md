# شرح مشروع BookNest

## 1. الملخص العام

مشروع **BookNest** هو واجهة متجر إلكتروني عربي متخصص في عرض الكتب وبيعها. يهدف المشروع إلى تقديم تجربة سهلة للقارئ تبدأ من اكتشاف الكتاب، ثم قراءة معلوماته، ثم إضافته إلى السلة أو قائمة المفضلة.

المشروع مصمم باللغة العربية ويدعم اتجاه الكتابة من اليمين إلى اليسار. كما أنه متجاوب مع شاشات الهاتف واللابتوب والشاشات الكبيرة.

> المشروع الحالي يمثل نسخة Frontend، أي واجهة المستخدم التي تظهر داخل المتصفح. أما تسجيل الدخول الحقيقي والدفع وحفظ الطلبات على خادم فتحتاج إلى Backend وقاعدة بيانات في مرحلة لاحقة.

## 2. فكرة المتجر ومشكلته

تواجه بعض المتاجر التقليدية صعوبة في عرض الكتب بطريقة منظمة تسمح للعميل بالبحث والمقارنة واتخاذ قرار الشراء بسرعة. لذلك يقدم BookNest واجهة رقمية تجمع الكتب في مكان واحد، وتقسمها حسب التصنيف، وتعرض السعر والمؤلف والوصف، وتمنح المستخدم أدوات لحفظ الكتب وإدارتها.

الفكرة الأساسية هي تحويل تجربة البحث التقليدية عن الكتب إلى تجربة إلكترونية واضحة ومريحة. يستطيع المستخدم الانتقال بين أقسام الموقع من خلال شريط التنقل العلوي دون الحاجة إلى فتح صفحات منفصلة.

## 3. أهداف المشروع

يحقق المشروع الأهداف التالية:

1. عرض متجر كتب عربي بتصميم حديث وسهل الاستخدام.
2. تنظيم الكتب داخل تصنيفات مثل التكنولوجيا والإدارة والأدب.
3. توفير بحث مباشر عن الكتب بالاسم أو المؤلف أو التصنيف.
4. توفير ترتيب للكتب حسب السعر.
5. تمكين المستخدم من مشاهدة تفاصيل كل كتاب.
6. تمكين المستخدم من إضافة الكتب إلى السلة.
7. تمكين المستخدم من حفظ الكتب في المفضلة.
8. حساب المجموع الفرعي وتكلفة الشحن والإجمالي.
9. توفير نموذج للتواصل مع إدارة المتجر.
10. توفير قسم حساب المستخدم كنموذج أولي لتسجيل الدخول.

## 4. أقسام الموقع

### الصفحة الرئيسية

تحتوي الصفحة الرئيسية على الشعار ورسالة تعريفية بالمتجر، بالإضافة إلى زر للوصول إلى الكتب وزر للتعرف على قصة المتجر. كما تعرض أرقامًا تعريفية مثل عدد الكتب وعدد القراء وتقييم المتجر.

تحتوي الصفحة أيضًا على تصنيفات مختارة. عند الضغط على تصنيف، ينتقل المستخدم إلى قسم الكتب ويتم تطبيق البحث المرتبط بالتصنيف.

### قسم الكتب

يعرض هذا القسم بطاقات الكتب. تحتوي كل بطاقة على صورة الكتاب، اسمه، المؤلف، التصنيف، السعر، زر الإضافة إلى السلة، زر المفضلة، وزر عرض التفاصيل.

يدعم القسم البحث الفوري. عندما يكتب المستخدم كلمة في حقل البحث، يتم تصفية الكتب التي تحتوي بياناتها على هذه الكلمة. كما يمكن ترتيب النتائج حسب السعر الأقل أو السعر الأعلى.

### قسم تفاصيل الكتاب

يعرض هذا القسم صورة الكتاب ومعلوماته الأساسية ووصفًا مختصرًا وأزرار التفاعل. يستطيع المستخدم إضافة الكتاب مباشرة إلى السلة أو المفضلة من خلال صفحة التفاصيل.

يتم إظهار القسم عند الضغط على زر التفاصيل، بينما يختفي قسم الكتب مؤقتًا. زر العودة يعيد المستخدم إلى قائمة الكتب.

### سلة المشتريات

تعرض السلة الكتب التي اختارها المستخدم. يستطيع المستخدم تعديل كمية كل كتاب أو حذفه من السلة.

يتم حساب قيمة السلة وفق المعادلة التالية:

```text
المجموع الفرعي = سعر كل كتاب × الكمية
الإجمالي = المجموع الفرعي + تكلفة الشحن
```

في النسخة الحالية، تكلفة الشحن ثابتة وتساوي 50 ريالًا عند وجود منتجات داخل السلة، وتصبح صفرًا عندما تكون السلة فارغة.

### قائمة المفضلة

تسمح المفضلة للمستخدم بحفظ الكتب التي يريد الرجوع إليها لاحقًا. يمكن إضافة الكتاب أو إزالته دون التأثير على السلة.

### قسم من نحن

يشرح هذا القسم فكرة BookNest وقيمته للمستخدم. يوضح أن المتجر يختار الكتب بعناية، ويهدف إلى تقريب الكتاب المناسب من القارئ المناسب.

### قسم الحساب

يحتوي على نموذج تسجيل دخول تجريبي. النموذج يعرض رسالة نجاح فقط ولا ينشئ حسابًا حقيقيًا، لأن المشروع لا يحتوي حاليًا على خادم أو قاعدة بيانات.

### قسم التواصل

يحتوي على عنوان المتجر ورقم الهاتف والبريد الإلكتروني ونموذج إرسال رسالة. عند إرسال النموذج تظهر رسالة تأكيد تجريبية، ولا يتم إرسال البيانات إلى خادم خارجي في النسخة الحالية.

## 5. شريط التنقل العلوي

تم تصميم الـNavbar ليكون نقطة الوصول الرئيسية لأقسام المتجر. يحتوي على:

- شعار BookNest.
- زر الرئيسية.
- زر الكتب.
- زر من نحن.
- زر اتصل بنا.
- زر حسابي.
- زر المفضلة.
- زر السلة.

كل زر يحتوي على أيقونة مناسبة. كما أن الأزرار لها حواف دائرية، وتأثير Hover، وظلال خفيفة، وتغير في اللون عند المرور عليها.

يتغير الزر النشط تلقائيًا أثناء التمرير بين الأقسام. وهذا يجعل المستخدم يعرف القسم الذي يقرأه حاليًا.

على شاشات اللابتوب، يتم تقليل المسافات وأحجام بعض العناصر حتى لا تتزاحم روابط التنقل. وعلى الهاتف يتحول الـNavbar إلى قائمة قابلة للفتح والإغلاق.

## 6. التقنيات المستخدمة

| التقنية | الاستخدام داخل المشروع |
|---|---|
| HTML5 | بناء هيكل الصفحة والأقسام والعناصر |
| CSS3 | الألوان، المسافات، الحواف، الظلال، الحركات، والتجاوب |
| JavaScript | السلة، المفضلة، البحث، الترتيب، التفاصيل، والنماذج |
| Bootstrap 5 RTL | الشبكة والتجاوب ودعم اتجاه RTL |
| Bootstrap Icons | الأيقونات المستخدمة في الأزرار والواجهة |
| Google Fonts | استخدام خط Cairo وخط Playfair Display |
| localStorage | حفظ السلة والمفضلة داخل المتصفح |

## 7. هيكل الملفات

```text
BookStore-single-page/
│
├── index.html
├── شرح-المشروع.md
│
├── css/
│   └── style.css
│
├── js/
│   └── script.js
│
└── images/
    ├── banner1.png
    ├── BOOK1.png
    ├── BOOK2.png
    ├── cat1.png
    ├── cat2.png
    ├── cat3.png
    ├── sample logo 1.png
    └── باقي الصور
```

### ملف index.html

هذا هو الملف الرئيسي للمشروع. يحتوي على جميع أقسام الموقع في صفحة واحدة، ويربط ملف التنسيق وملف JavaScript ومجلد الصور.

### ملف style.css

يحتوي على كل التنسيقات المرئية للمشروع. يتضمن ألوان المتجر، الحواف الدائرية، تأثيرات Hover، الظلال، أحجام الخطوط، تنسيق الـNavbar، وتنسيق الشاشات المختلفة.

### ملف script.js

يحتوي على منطق التفاعل. يدير بيانات الكتب، ويضيف العناصر إلى السلة، ويعدل الكميات، ويحذف المنتجات، ويحدث أعداد السلة والمفضلة، وينفذ البحث والترتيب وعرض التفاصيل.

### مجلد images

يحتوي على صور الشعار والكتب والتصنيفات والصور المستخدمة في الواجهة.

## 8. طريقة عمل JavaScript

يتم تعريف الكتب داخل مصفوفة JavaScript. كل كتاب يحتوي على معرف، واسم، ومؤلف، وسعر، وتصنيف، وصورة، ووصف.

عند إضافة كتاب إلى السلة، يتم حفظ المعرف والكمية في `localStorage` تحت المفتاح `cart`. وعند فتح أو تحديث قسم السلة، يقرأ البرنامج هذه البيانات ثم يبحث عن تفاصيل الكتب داخل مصفوفة الكتب.

يتم حفظ المفضلة في `localStorage` تحت المفتاح `wishlist`. وهذا يسمح ببقاء البيانات عند تحديث الصفحة أو الانتقال بين الأقسام، ما دام المستخدم يستخدم نفس المتصفح.

## 9. معنى localStorage وحدوده

`localStorage` هو مساحة تخزين داخل المتصفح. يستخدم المشروع هذه المساحة لحفظ السلة والمفضلة دون الحاجة إلى خادم.

هذه الطريقة مناسبة للنماذج الأولية والمشاريع التعليمية، لكنها ليست مناسبة لمتجر تجاري كامل للأسباب التالية:

- البيانات محفوظة على جهاز المستخدم فقط.
- لا يمكن الوصول إلى السلة من جهاز آخر.
- حذف بيانات المتصفح يؤدي إلى حذف البيانات.
- لا يوجد حساب حقيقي مرتبط بالبيانات.
- لا يوجد تحقق أمني أو حماية للطلبات.

## 10. الهوية البصرية

تم اختيار هوية تجمع بين البنفسجي والبرتقالي. البنفسجي يعطي إحساسًا بالمعرفة والثقة، بينما يضيف البرتقالي الحيوية ويجذب الانتباه إلى الأزرار المهمة.

تستخدم الواجهة خط Cairo للنصوص العربية وخط Playfair Display للاسم والعناوين البارزة. كما تستخدم البطاقات حوافًا دائرية وظلالًا ناعمة حتى تبدو الواجهة أكثر حداثة وراحة.

## 11. التشغيل المحلي

بعد فك ضغط المشروع، يتم تشغيله بالطريقة البسيطة التالية:

1. افتح مجلد `BookStore-single-page`.
2. افتح ملف `index.html` في المتصفح.
3. ستظهر الصفحة الرئيسية مباشرة.
4. استخدم شريط التنقل للانتقال بين الأقسام.

يمكن أيضًا تشغيله من خلال خادم محلي لتحسين عمل الروابط والملفات:

```bash
cd BookStore-single-page
python3 -m http.server 4174
```

ثم افتح العنوان التالي في المتصفح:

```text
http://localhost:4174
```

## 12. ما تم تنفيذه فعليًا

النسخة الحالية تحتوي على واجهة موحدة في ملف HTML واحد. تم ربط ملف CSS وملف JavaScript ومجلد الصور بشكل صحيح.

كما تم تنفيذ السلة والمفضلة والبحث والترتيب وعرض التفاصيل وتحديث أعداد المنتجات. وتم تحسين الـNavbar ليكون واضحًا على اللابتوب والهاتف، مع دعم الحواف الدائرية وتأثيرات Hover والألوان والأيقونات.

تم كذلك اختبار صياغة JavaScript والتحقق من سلامة الأرشيف المضغوط.

## 13. ما يحتاج إلى Backend مستقبلًا

لتحويل المشروع إلى متجر حقيقي، يجب إضافة خادم خلفي وقاعدة بيانات. الخادم الخلفي مسؤول عن استقبال البيانات وتنفيذ العمليات التي لا يجب أن تبقى داخل المتصفح.

الوظائف المطلوبة مستقبلًا تشمل:

- إنشاء حسابات المستخدمين وتسجيل الدخول الحقيقي.
- حفظ الكتب والمستخدمين والطلبات في قاعدة بيانات.
- إنشاء لوحة تحكم لإدارة الكتب.
- ربط السلة بحساب المستخدم.
- إنشاء الطلبات ومتابعة حالتها.
- ربط الدفع الإلكتروني.
- إرسال رسائل البريد الإلكتروني.
- حماية البيانات والتحقق من الصلاحيات.

يمكن تنفيذ هذه المرحلة باستخدام Node.js أو Laravel أو PHP، مع قاعدة بيانات MySQL أو PostgreSQL.

## 14. مقترحات تحسين المشروع

يفضل في المرحلة القادمة إضافة صفحة إدارة للمتجر حتى يستطيع المسؤول إضافة الكتب وتعديل الأسعار والصور دون تعديل ملفات HTML يدويًا.

كما يفضل إضافة نظام تقييم ومراجعات للكتب، وإضافة فلترة حسب السعر والتصنيف والمؤلف، وإضافة قسم للعروض والكتب الجديدة.

من الناحية البصرية، يمكن تحسين المشروع بإضافة شعار SVG احترافي، وحركات ظهور بسيطة عند التمرير، وزر واتساب عائم، وقسم لآراء العملاء، ووضع داكن اختياري.

من ناحية الاستخدام، سيكون من المفيد إضافة رسالة تأكيد داخل الواجهة بدل رسائل المتصفح التقليدية، وإظهار حالة فارغة جميلة للسلة والمفضلة، وإضافة مؤشر تحميل عند تنفيذ العمليات.

## 15. الخلاصة

BookNest هو نموذج متجر كتب إلكتروني عربي يركز على تنظيم الكتب وتسهيل اكتشافها. يجمع المشروع بين التصميم المتجاوب والوظائف التفاعلية الأساسية في صفحة واحدة، مع فصل التنسيق في ملف CSS والمنطق البرمجي في ملف JavaScript.

النسخة الحالية مناسبة للعرض والتقييم والمشاريع التعليمية والنماذج الأولية. ولكي تصبح منصة بيع حقيقية، تحتاج إلى ربط Backend وقاعدة بيانات ونظام مستخدمين ودفع إلكتروني.

## المراجع

[1]: https://developer.mozilla.org/en-US/docs/Web/HTML "MDN HTML Documentation"

[2]: https://developer.mozilla.org/en-US/docs/Web/CSS "MDN CSS Documentation"

[3]: https://developer.mozilla.org/en-US/docs/Web/JavaScript "MDN JavaScript Documentation"

[4]: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage "MDN localStorage Documentation"

[5]: https://getbootstrap.com/docs/5.3/getting-started/introduction/ "Bootstrap 5.3 Documentation"
